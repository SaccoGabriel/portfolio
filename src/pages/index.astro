---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import Header from '../components/Header.astro';


---

<Layout title="Portfolio Gabriel SACCO">
	<main>
		<div>
			<Header
				title="Salut l'ékip 👋"
				subtitle="UwU"
				body= ""
				job="Valideur"
			/>
		
		</div>
	</main>
</Layout>

<style>

	.astro-a {
		position: absolute;
		top: -32px;
		left: 50%;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1;
	}

	.text-gradient {
		background-image: var(--background);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		color:white;
		background-position: 0%;
	}
	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
		padding: 1.5rem;
		border-radius: 8px;
	}
	.instructions code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.instructions strong {
		color: rgb(var(--accent-light));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 2rem;
		padding: 0;
	}


/*.smiley:hover {

    animation: bounce 1s infinite alternate; /* Utilisation de l'animation nommée 'bounce' 
}
/* Définition de l'animation 'bounce' 
@keyframes bounce {
    0% {
        transform: rotate(0deg); /* Pas de déplacement vertical 
    }
    40% {
        transform: rotate(20deg); /* Déplacement vers le bas de 20px 
    }
}
*/



</style>
